- pipeline.id: ingest
  pipeline.workers: 4
  pipeline.batch.size: 1000
  pipeline.batch.delay: 50
  pipeline.ecs_compatibility: disabled
  pipeline.ordered: false
  queue.type: memory
  path.config: "/root/pipelines"
  config.reload.automatic: true
  config.reload.interval: 3s